{
  "name": "mhasan-me-tooling",
  "private": true,
  "description": "mhasan.me site source (Eleventy build) and build-time tooling (OG image generation, migration/verification scripts).",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "cheerio": "^1.2.0",
    "diff": "^9.0.0",
    "sharp": "^0.35.4"
  }
}
